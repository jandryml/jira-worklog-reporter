Simple project for importing worklogs from csv to Jira.

1. Fill in correct configuration.
   1. Copy `config-temple.properties` to `config.properties`.
   2. Fill in correct values to `config.properties` file.